{
  "name": "http-probe",
  "version": "2.2.0",
  "description": "Utility for HTTP validation. Implementation is based on the Chrome debugging protocol.",
  "main": "./src/index.js",
  "scripts": {
    "coveralls": "nyc npm test && nyc report --reporter=text-lcov | coveralls && rm -rf ./.nyc_output",
    "test": "mocha -r ./test/setup --recursive"
  },
  "engines": {
    "node": ">=14"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NicolasSiver/http-probe.git"
  },
  "keywords": [
    "chrome",
    "debbuging",
    "selenium",
    "http",
    "mock",
    "validate",
    "webdriver"
  ],
  "author": "Nicolas Siver",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/NicolasSiver/http-probe/issues"
  },
  "homepage": "https://github.com/NicolasSiver/http-probe#readme",
  "devDependencies": {
    "chai": "^4.3.4",
    "chrome-remote-interface": "^0.31.0",
    "coveralls": "^3.1.1",
    "mocha": "^9.1.2",
    "mocha-lcov-reporter": "^1.3.0",
    "nyc": "^15.1.0",
    "sinon": "^11.1.2"
  }
}
